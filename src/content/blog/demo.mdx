---
title: "Style demo"
date: 2026-04-06
description: "A kitchen-sink post to preview all content styles"
hidden: true
---

This is a regular paragraph. It has **bold text**, *italic text*, and `inline code`. You can also combine them like **`bold code`** or *`italic code`*. Links look [like this](https://example.com).

## Heading two

This paragraph follows a second-level heading. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

### Heading three

And this one follows a third-level heading. Keep going to see all the components below.

---

## Code blocks

A fenced code block with syntax highlighting:

```typescript
const name = 'Thomas';
console.log(name);
```

---

## Blockquotes

> The purpose of abstracting is not to be vague, but to create a new semantic level in which one can be absolutely precise.
>
> — Edsger W. Dijkstra

---

## Lists

An unordered list:

- First item in the list
- Second item, slightly longer to show wrapping behaviour on narrow viewports
- Third item with `inline code` inside it
- Fourth item with **bold** and *italic* text

An ordered list:

1. Parse the raw bytes into a structured format
2. Validate the header flags and opcode
3. Iterate over question records
4. Resolve each record against the local cache
5. Forward cache misses to the upstream resolver

---

## Inline elements

Here is a sentence that uses **strong emphasis**, *soft emphasis*, `monospace code`, and a [hyperlink](https://example.com) all together. The spacing and colour of each should feel consistent with the surrounding text weight.

A second paragraph so you can see the gap between them. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.

---

## Horizontal rule

Above and below this paragraph you can see how `<hr>` renders with the border colour.

---

That is the end of the demo.
